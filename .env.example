# Optional. Point at a Formspree or Web3Forms endpoint to have the contact form POST there.
# Leave unset and the form composes a message in the visitor's own mail client instead.
VITE_CONTACT_ENDPOINT=
